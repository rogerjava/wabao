server:
  port: ${SERVER_PORT:18080}

spring:
  datasource:
    url: jdbc:sqlite:${WABAO_DB_PATH:./data/wabao.db}
    driver-class-name: org.sqlite.JDBC
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    root: INFO
